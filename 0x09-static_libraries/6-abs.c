#include "main.h"
/**
 * _abs- a function that prints the absolute value of an integer
 * @n: the int to be checked
 * Return: 1 Success
 */
int _abs(int n)
{
	if (n < 0)
	{
		return (-n);
	}
	else
	{
		return (n);
	}
}
